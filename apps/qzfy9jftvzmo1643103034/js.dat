import Subtest from './Subtest';
const { useState, useEffect } = React;

const Test = () => {
    const [value, setValue] = useState("");

    useEffect(() => {
        console.log(wiz)
        console.log(wiz['component.react.testapp'])
    }, []);

    return (
        <div>
            this is test app!!!!!!
            <Subtest
                value={value}
                setValue={setValue}
            />
        </div>
    );
}

export default Test;
